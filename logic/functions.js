function calculateVolume(sets, reps, weight) {
    return sets * reps * weight;
}
function addExercise(exercise) {
    // Implementation to add an exercise
}
function removeExercise(exercise) {
    // Implementation to remove an exercise
}
